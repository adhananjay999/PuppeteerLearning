{
  "name": "puppeteerlearning",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./dist",
    "linux-clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.7",
    "dotenv": "^14.2.0",
    "puppeteer": "^13.0.1"
  },
  "devDependencies": {
    "@types/config": "0.0.41",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
